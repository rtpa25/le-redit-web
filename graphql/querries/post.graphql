# @format

query Posts($limit: Int!, $cursor: Int) {
  posts(cursor: $cursor, limit: $limit) {
    posts {
      id
      title
      createdAt
      text
      textSnippet
    }
    hasMorePosts
  }
}
